apiVersion: apps/v1
kind: ReplicaSet
metadata:
  name: my-replica-set
spec:
  replicas: 3
  selector:
    matchLabels:
      app: myapp
  template:
    metadata:
      name: my-new-pod
      labels:
        app: myapp
    spec:
      containers:
        - name: new-container
          image: nginx:latest
          ports:
            - containerPort: 80
